import random

while True:
	def my_array(length=8):
		numbers  = '0123456789'
		letters	 = 'abcdefghijklmnoprstuvz'
		return ''.join(random.choice(numbers)			# numbers
		#return ''.join(random.choice(letters)			# letters
		#return ''.join(random.choice(numbers + letters) 	# numbers and letters
	for i in range(length))
	print (my_array(8))
